pipeline {
    agent any

    stages {
        stage('Build') {
            stages {
                stage('Compile') {
                    steps {
                        script {
                            def someCondition = true
                            if (someCondition) {
                                error("Stopping the pipeline: Validation failed!")
                            }
                        }
                    }
                }
            }
        }
    }
}
